#include <stdio.h>

int main() {
    int sum = 0;

    printf("The pattern is:\n");

    for (int i = 1; i <= 5; i++) {
        sum = sum * 10 + 1;

        for (int k = 1; k <= 5 - i; k++) {
            printf(" ");
        }

        printf("%d\n", sum * sum);
    }

    return 0;
}
